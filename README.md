#metashock / stap-scripts

A collection of SystemTap scripts

## dnstop
Prints system-wide DNS queries along with the issuing process and it's PID. The ouput scheme is as follows:

 EXECNAME(PID) URL_OR_IP


